import{_ as t,o as a,c as o,ae as s}from"./chunks/framework.D3uQw3tB.js";const g=JSON.parse('{"title":"Settings","description":"","frontmatter":{},"headers":[],"relativePath":"settings.md","filePath":"settings.md"}'),n={name:"settings.md"};function i(r,e,l,h,c,d){return a(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="settings" tabindex="-1">Settings <a class="header-anchor" href="#settings" aria-label="Permalink to &quot;Settings&quot;">​</a></h1><p>Most people open Settings once, maybe to switch to a dark theme, and then never come back. That&#39;s by design — the defaults are tuned to be sensible, and you can build decks all day without touching anything here. But there are a handful of knobs that genuinely change how the app behaves, and a few that unlock the AI features entirely. This page walks through every section in order, explaining not just what each setting does but when you&#39;d actually want to reach for it.</p><p>The Settings page is split into seven sections down the left-hand sidebar. I&#39;ll take them one at a time.</p><h2 id="general" tabindex="-1">General <a class="header-anchor" href="#general" aria-label="Permalink to &quot;General&quot;">​</a></h2><p>This is where you pick how the app looks. There&#39;s a helper line at the top of the pane that says it best: &quot;Pick a visual theme. Light and dark themes are grouped — toggle the mode to see the other set.&quot; So the first thing to know is that there&#39;s a Light/Dark mode toggle, and flipping it doesn&#39;t switch your theme directly — it swaps which <em>set</em> of themes you&#39;re choosing from. The light themes and dark themes are separate collections, and you pick one theme out of whichever set is currently showing.</p><p>There are six themes in total, and they&#39;re more than recolored buttons — each one carries its own typographic voice, leaning into a particular Magic plane and a real-world print tradition. On the light side you&#39;ve got:</p><ul><li><strong>Strixhaven</strong> (&quot;Editorial&quot;) — a warm off-white with muted terracotta accents, styled like a curator&#39;s catalog. This is the default light theme, and it&#39;s the most neutral of the bunch if you just want something clean.</li><li><strong>Dominaria</strong> (&quot;Grimoire&quot;) — parchment and oxblood, meant to feel like a leather-bound spellbook.</li><li><strong>Kaladesh</strong> (&quot;Drafting Table&quot;) — drafting cream and aged brass, an engineer&#39;s ledger.</li><li><strong>Kamigawa</strong> (&quot;Woodblock&quot;) — washi cream with indigo and a vermillion seal.</li></ul><p>And on the dark side:</p><ul><li><strong>Neo Kamigawa</strong> (&quot;Dataslate&quot;) — black with a blue cast and a hot accent color, like a terminal readout.</li><li><strong>Innistrad</strong> (&quot;Candlelight&quot;) — candle-bone text on dried-ink black with garnet accents. This is the default dark theme.</li></ul><p>Pick whichever suits the room you&#39;re in. Clicking a theme card applies it instantly — there&#39;s no save button to hunt for, and no restart. If you spend evenings deckbuilding, the dark themes are easier on the eyes; if you&#39;re working in a bright room or want something that reads like a reference book, the light set is the way to go.</p><h2 id="set-collection" tabindex="-1">Set Collection <a class="header-anchor" href="#set-collection" aria-label="Permalink to &quot;Set Collection&quot;">​</a></h2><p>This one exists to make card search actually useful for <em>your</em> collection rather than the whole of Magic. The helper text spells out the purpose: &quot;Track which MTG sets you own cards from. Collection levels determine which card rarities are included in Scryfall filters.&quot; The reason that matters is that when you ask the app — or an AI assistant connected to it — to find cards, it can narrow the results to what you&#39;re likely to already own instead of suggesting a pile of cards you&#39;d have to go buy. If you&#39;ve got a shoebox full of a few sets and want decks you can build tonight, this is how you tell the app what&#39;s in the box.</p><p>You add sets by searching for them and adding them one at a time. Each set you add gets a collection <strong>Level</strong>, which is a rough estimate of how deep your ownership of that set goes. The levels map to rarities like this:</p><ul><li><strong>Level 1</strong> — &quot;Few packs - commons and uncommons.&quot; You cracked a few packs; assume you&#39;ve got the common stuff.</li><li><strong>Level 2</strong> — &quot;Moderate - commons, uncommons, and rares.&quot; This is the default applied to every new set you add, and it&#39;s a reasonable middle ground.</li><li><strong>Level 3</strong> — &quot;Good collection - all except mythics.&quot; You&#39;ve got most of the set, just not the chase cards.</li><li><strong>Level 4</strong> — &quot;Complete - all cards.&quot; You own everything from this set, mythics included.</li></ul><p>The point of the levels is honesty over optimism. If you set everything to Level 4 you&#39;ll get suggestions full of expensive mythics you don&#39;t actually have, which defeats the purpose. Set each one to what&#39;s realistically true and the filters earn their keep.</p><p>Once you&#39;ve added sets, they show up in a grouped table — Expansions, Core Sets, and so on — with each row showing the set&#39;s code, release year, name, its level (which you can change inline), and a delete button if you want to drop a set you no longer track.</p><h2 id="roles" tabindex="-1">Roles <a class="header-anchor" href="#roles" aria-label="Permalink to &quot;Roles&quot;">​</a></h2><p>Roles are how you tag a card by the <em>job</em> it does in a deck — Ramp, Removal, Card Draw, Win Condition, that kind of thing — so that later you can look at a deck and immediately see whether it&#39;s actually got enough interaction or whether you&#39;ve accidentally built a pile of win-cons with no way to survive to play them. The helper line here reads: &quot;Global roles are available across all decks. Hover for details, click to edit.&quot;</p><p>The word <em>global</em> is the key distinction. Roles defined on this page are available across every deck you build. There are also per-deck roles, but you manage those from inside a deck, not here — this section is strictly for the shared vocabulary you want available everywhere.</p><p>To add one, click <strong>Add Role</strong> and give it a name, an optional description, and a color picked from a palette. The color is what shows up as the little dot on the role pill, so it&#39;s worth choosing something distinct. Each existing role appears as a pill; click a pill to edit it, and hover over it to see usage stats — the tooltip tells you something like &quot;Used by X cards in Y decks,&quot; which is a quick way to spot a role you defined once and never actually used. Deleting a role warns you if it&#39;s currently in use; if you delete it anyway, the cards that had it keep the underlying role assignment, but the role itself drops off the list. That&#39;s a deliberate safety valve so a delete doesn&#39;t silently scrub data off your cards.</p><h2 id="mcp-server" tabindex="-1">MCP Server <a class="header-anchor" href="#mcp-server" aria-label="Permalink to &quot;MCP Server&quot;">​</a></h2><p>This section is the on switch for the AI features. The whole reason the app is interesting beyond a deck spreadsheet is that you can connect it to an AI assistant and then <em>talk</em> to your collection — &quot;build me a mono-green ramp deck,&quot; &quot;what&#39;s the mana curve on my Isshin deck,&quot; &quot;which decks have Sol Ring in them&quot; — and the assistant reads and edits your decks directly. None of that works until you flip the switch here.</p><p>There are three integration cards, one per assistant, and connecting is a single click each:</p><ul><li><strong>Claude Desktop</strong> — click <strong>Connect to Claude</strong>. This writes the configuration Claude Desktop needs so that Claude can manage your decks. One thing to note: you may need to restart Claude Desktop for the change to take effect.</li><li><strong>Claude Code</strong> — click <strong>Connect to Claude Code</strong>.</li><li><strong>Gemini CLI</strong> — click <strong>Connect to Gemini CLI</strong>.</li></ul><p>Each card shows whether it&#39;s currently connected or disconnected, and once connected it offers a <strong>Disconnect</strong> button if you want to turn it back off. You don&#39;t need to understand anything about how the plumbing works — connecting just tells that assistant where to find your decks. For what you can actually <em>do</em> once you&#39;re connected, see <a href="/decklist-tools/usage-mcp.html">Building with AI assistants</a>.</p><h2 id="skills" tabindex="-1">Skills <a class="header-anchor" href="#skills" aria-label="Permalink to &quot;Skills&quot;">​</a></h2><p>Connecting an assistant in the previous section gives it the <em>ability</em> to touch your decks. Skills make it <em>good</em> at it. The helper text puts it precisely: &quot;Install MTG Deckbuilder skills into your AI clients. Skills teach the assistant how to use the MCP server effectively — what queries to run, what tools to chain, and the vocabulary specific to deckbuilding.&quot; Without skills, the assistant can fumble its way through; with them, it knows the right moves — how to phrase a Scryfall search, when to chain a couple of tools together, the deckbuilding terms you&#39;d actually use.</p><p>This section is a table where you install, update, or uninstall skills per client (Claude Code and Gemini CLI), and there are bulk &quot;All&quot; actions if you&#39;d rather not click through them one by one. There&#39;s also an Export option — a <strong>Save…</strong> button — that writes the skills out as a SKILL.md bundle in a .zip file. That export exists mainly for Claude Desktop, which doesn&#39;t install skills the same way: for Claude Desktop, open the app&#39;s Capabilities settings and upload the zip there.</p><p>For a description of what each individual skill teaches the assistant, see the <a href="/decklist-tools/skills.html">Skills</a> page.</p><h2 id="data" tabindex="-1">Data <a class="header-anchor" href="#data" aria-label="Permalink to &quot;Data&quot;">​</a></h2><p>This is your backup, restore, and move-to-a-new-computer tool. Everything you&#39;ve built lives in files on your machine (more on that at the end of this page), and that means a backup is on you. This section is how you make one. The helper text notes an important caveat: &quot;Export your entire collection to a backup file, or restore from a previous backup. Cache data and images are excluded — use &#39;Load All Cards&#39; to rebuild those after importing.&quot; In other words the backup carries your <em>work</em> — decks, roles, settings, interest list, set collection — but not the downloaded card data and images, because those can always be re-fetched and would bloat the file enormously. You rebuild the cache afterward from the Cache section.</p><p><strong>Export Collection</strong> writes a single JSON backup containing your decks, roles, settings, interest list, and set collection. Stash that somewhere safe — a cloud drive, a USB stick — and you&#39;ve got a restore point or a way to carry your collection to another computer.</p><p><strong>Import Collection</strong> is the other half, and it&#39;s the one to be careful with. Importing <strong>replaces all of your existing data</strong> — decks, roles, settings, interest list — with whatever&#39;s in the backup file, and it cannot be undone. If you&#39;ve done any work since your last backup, that work is gone the moment you import. So before you import anything, export a fresh backup first. Used correctly this is exactly what you want for restoring after a wipe or setting up a second machine; used carelessly it&#39;ll happily erase a deck you forgot to save elsewhere.</p><h2 id="cache" tabindex="-1">Cache <a class="header-anchor" href="#cache" aria-label="Permalink to &quot;Cache&quot;">​</a></h2><p>Card data and images don&#39;t live in the app — they come from Scryfall, the community card database. Fetching all of that over the network every single time you opened a deck would be slow and would fall over the moment you lost your connection. So the app keeps a local cache: once it&#39;s pulled a card, it hangs onto the data and the image so the next view is instant and works offline. This section is where you see what&#39;s cached and manage it.</p><p>At the top you get cache <strong>statistics</strong> — how many cards&#39; worth of data you&#39;ve cached and how much space it takes, the same for images, the totals, and the oldest and newest entries so you can see how stale things have gotten.</p><p><strong>Load All Cards</strong> is the pre-fetch button. It goes through every card in your decks and pulls everything down ahead of time, so you&#39;re not waiting on the network later — handy right before a trip, or right after importing a backup since the import doesn&#39;t bring the cache along. You can choose <strong>Card Data Only</strong> or <strong>Card Data + Images</strong>, and it runs with a progress bar and a cancel button so you&#39;re never stuck waiting on a long pull.</p><p>Below that are a few settings, each with a real reason behind it:</p><ul><li><strong>Enable Image Caching</strong> — on by default. This is what lets you view card images offline. Turn it off only if you&#39;re tight on disk space and don&#39;t care about seeing the art when you&#39;re disconnected.</li><li><strong>Max Image Cache Size (MB)</strong> — defaults to 500, and you can set it anywhere from 100 to 10000. The limit exists because images add up fast and you don&#39;t want the cache growing without bound. When you exceed the cap, the app drops the oldest images to make room, so raising it just means you keep more art around before that pruning kicks in.</li><li><strong>Cache Expiry (Days)</strong> — defaults to 7, range 1 to 365. Card data older than this gets re-fetched the next time it&#39;s needed. The reason it expires at all is that printings and prices drift over time; a shorter expiry keeps that information fresh at the cost of a few more network calls, a longer one trades freshness for fewer fetches.</li></ul><p>Finally there are maintenance actions for when something seems off or you want to reclaim space. <strong>Clear Card Data</strong>, <strong>Clear Images</strong>, and <strong>Clear All Cache</strong> each wipe the corresponding part of the cache — nothing is lost permanently since it all re-fetches from Scryfall as needed, you&#39;ll just hit the network again the next time you view those cards. <strong>Rebuild Index</strong> repairs the cache&#39;s internal bookkeeping if the statistics ever look wrong or a cached card isn&#39;t being found; it doesn&#39;t re-download anything, it just fixes the metadata that tracks what&#39;s stored.</p><h2 id="where-your-data-lives" tabindex="-1">Where your data lives <a class="header-anchor" href="#where-your-data-lives" aria-label="Permalink to &quot;Where your data lives&quot;">​</a></h2><p>One thing worth knowing across all of the above: everything stays on your computer. Your decks, your settings, and the cached card data all live in local files on your machine — nothing is uploaded to a server, and the app works perfectly well with no internet beyond the occasional Scryfall fetch to fill the cache. That&#39;s the upside, and it&#39;s also exactly why the <a href="#data">Data</a> section matters: since there&#39;s no cloud copy quietly backing you up, exporting a backup now and then is the only thing standing between you and losing your work if something happens to the machine.</p>',42)])])}const p=t(n,[["render",i]]);export{g as __pageData,p as default};
